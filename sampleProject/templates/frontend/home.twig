{%  extends 'base.twig' %}

{% block htmlHeadCss %}
    <link href="/css/frontend.css" rel="stylesheet" type="text/css">
{% endblock %}

{%  block htmlBodyContent %}
    <main>
        <h1>{{ businessName }}</h1>
        {% if notice %}
            <div class="{{ notice.1 }}">{{ notice.0|raw }}</div>
        {% endif %}
    </main>

    {% include 'frontend/partials/navigation.twig' %}

{%  endblock %}