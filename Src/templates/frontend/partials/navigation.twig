	<header>
		<nav>

            {% if currentUri != '/' %}
			<li><a href="{{ path_for(constant('ROUTE_HOME'))}}">Home</a></li>
            {% endif %}

	{% if not authentication.check %}
			<li><a href="{{ path_for(constant('ROUTE_LOGIN'))}}">Login</a></li>
    {% else %}
            <li><a href="{{ path_for(adminLinkRoute) }}">Admin</a></li>
	{% endif %}

		</nav>
	</header>
